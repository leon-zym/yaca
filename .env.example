# Optional. yaca defaults to ~/.yaca and never uses ~/.pi for its runtime data.
YACA_HOME=/absolute/path/to/.yaca

# Reserved for the later Pi adapter slice. Keep credentials in the process environment.
DEEPSEEK_API_KEY=
